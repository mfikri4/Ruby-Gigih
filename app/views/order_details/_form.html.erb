<%= form_with(model: order_detail) do |form| %>
  <% if order_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_detail.errors.count, "error") %> prohibited this order_detail from being saved:</h2>

      <ul>
        <% order_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :OrderId, style: "display: block" %>
    <%= form.collection_select(:order_id, Order.all, :id, :customer_id, :prompt => 'Select') %>
  </div>

  <div>
    <%= form.label :MenuId, style: "display: block" %>
    <%= form.collection_select(:menu_id, Menu.all, :id, :name, :prompt => 'Select') %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.text_field :quantity %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
